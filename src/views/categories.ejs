<%- include('partials/header') %>

    <main>
        <h1>
            <%= title %>
        </h1>
        <h2>Service Projects Categories</h2>

        <ul>
            <% categories.forEach(cat=> { %>
                <li>
                    <%= cat.name %>
                </li>
                <% }) %>
        </ul>
    </main>

    <%- include('partials/footer') %>